<Window x:Class="tman0.Launcher.UI.DebugLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Minecraft Console" Height="300" Width="300" Topmost="True"
        WindowStyle="ToolWindow" Loaded="Window_Loaded_1">
    <Grid>
        <ScrollViewer Margin="0,0,0,30" HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" CanContentScroll="True"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Visible"
                      Name="Scroller">
            <TextBlock Name="Log" FontFamily="Consolas" TextWrapping="Wrap" />
        </ScrollViewer>
        <CheckBox Name="AutoScroll" Content="Auto Scroll" HorizontalAlignment="Left" Margin="5,0,0,6" VerticalAlignment="Bottom" Style="{StaticResource glassCheckbox}"/>
        <Button Name="Close" Content="Close" HorizontalAlignment="Right" Margin="0,0,0,3" VerticalAlignment="Bottom" Width="75" Click="Button_Click_1"/>
    </Grid>
</Window>
